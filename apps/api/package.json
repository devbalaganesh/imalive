{
  "name": "api",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch index.ts",
    "build": "bun build index.ts --outdir dist --target node",
    "start": "bun run ./dist/index.js",
    "postinstall": "mkdir -p prisma && cp ../../packages/db/prisma/schema.prisma ./prisma/schema.prisma && prisma generate"
  },
  "dependencies": {
    "@prisma/client": "6.2.1",
    "prisma": "6.2.1",
    "express": "^4.21.2",
    "cors": "^2.8.5",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@repo/db": "*"
  }
}
